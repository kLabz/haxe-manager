#!/bin/sh

ROOT=$(dirname $(readlink -f $0))/..
BUILD_OS="linux64"
if [[ "$OSTYPE" == "darwin"* ]]; then
	BUILD_OS="mac"
fi

HAXE_STD_PATH=$ROOT/std $ROOT/build/${BUILD_OS}_569e52e/haxe --cwd $ROOT run-select.hxml $@
